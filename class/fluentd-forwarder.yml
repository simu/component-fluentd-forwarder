parameters:
  kapitan:
    compile:
      - input_paths:
          - fluentd-forwarder/component/app.jsonnet
        input_type: jsonnet
        output_path: apps/
      - input_paths:
          - fluentd-forwarder/component/main.jsonnet
        input_type: jsonnet
        output_path: fluentd-forwarder/${_instance}/
